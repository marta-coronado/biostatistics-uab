---
title: ""
output:
  html_document:
    css: styles.css
---

```{r setup, include=FALSE}
options(htmltools.dir.version = FALSE)
#knitr::include_graphics()
knitr::opts_chunk$set(
  cache = TRUE,
  message = FALSE, 
  warning = FALSE,
  hiline = TRUE,
  fig.retina = 5
)
#pagedown::chrome_print(".html")
```
  
<br>

  
```{r echo = F}
library(dplyr)
library(knitr)
library(kableExtra)

# --- Parámetros ---
df1_vals <- 1:15      # grados de libertad del numerador
df2_vals <- 1:42     # grados de libertad del denominador
alpha <- 0.05        # nivel de significación (cola superior)

# --- Calcular valores críticos F ---
F_table <- sapply(df1_vals, function(df1) qf(1 - alpha, df1 = df1, df2 = df2_vals))

# --- Convertir en data frame ---
F_table_df <- data.frame(df2 = df2_vals, round(F_table, 4))
colnames(F_table_df) <- c("df2", paste0("gl1=", df1_vals))
rownames(F_table_df) <- paste0("gl2=",rownames(F_table_df))
F_table_df$df2<-rownames(F_table_df)
rownames(F_table_df) <- NULL

F_table_df <- F_table_df %>%
  rename(`Grados libertad` = df2)

# --- Mostrar tabla ---
kable(F_table_df, row.names = F, align = "c",
      caption = "Valores críticos de la distribución F (cola superior, α = 0.05)") %>%
  kable_styling(
    full_width = FALSE,
    position = "center",
    font_size = 9,  # letra pequeña
    bootstrap_options = c("condensed")
  ) %>%
  row_spec(0, bold = TRUE, background = "#e6f2ff") %>%
  column_spec(1, bold = TRUE, background = "#e6f2ff", width = "1.5cm")
```
  
  